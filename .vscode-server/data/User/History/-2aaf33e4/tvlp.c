#include <stdio.h>
#include <time.h>
#include <stdlib.h>
#include <string.h>
#include <sys/types.h>
#include <sys/stat.h>

void main(){

char str[100];
 char info[100];
 int infot[50];
 int tok;
 char tokk;
 printf("Enter file name: ");
 scanf("%s",str); 
  //  printf("%s",str);
// printf(str);
//  char number[]=();
 FILE* input = fopen(str,"r");
//  fscanf(input,"%s", info);
// fgets(info,100,input);
}